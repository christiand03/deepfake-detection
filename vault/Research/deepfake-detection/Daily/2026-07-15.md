---
type: daily
project: deepfake-detection
status: active
created: 2026-07-15T00:00:00Z
updated: 2026-07-15T00:00:00Z
---

# Daily - 2026-07-15

## Focus

Analyze the completed Phase 3 robustness sweeps (unimodal + multimodal) and add
them to the vault.

## Progress

- Validated provenance: `artifacts/robustness_*_recovered.csv` byte-match the W&B
  `sweep_results` tables (runs 2iftksg1 unimodal, kcdxce multimodal); both on the
  same 1471-video test split.
- Confirmed clean baselines and label balance (video/mm 1043 fake / 428 real;
  audio 701 / 770) → no degenerate-AUC risk.
- Ran strict analysis (Hanley-McNeil AUC CIs, degradation curves, mm−video gap),
  produced 3 figures.
- New Results note: [[Results/phase3-robustness-social-media-sweep]] (registered
  as results-006, added to [[02-Index]]).

## Setup-consistency audit (afternoon)

- Checked vault `experimental-setup-de.md` vs thesis `template/05ExperimentalSetup.tex`:
  they **match each other**, but both carried the **stale 2026-06-13 dataset table**
  (32 id / 11 886 vid / test 1 169) — conflicting with the Phase 3 note's 1 471 test.
- Ground truth (current CSVs, 2026-07-15): **165 id / 12 335 vid**; train 119/9 482,
  val 22/1 382, test 24/**1 471**. Identity-disjoint (verified, no leakage).
- Provenance clarified by user: **Phase 1/2 trained on the 32-id stage**; **Phase 3/4
  evaluate on the 165-id test split**. Confirmed leakage-free — deterministic
  per-identity hash (seed=11): test buckets 0.007–0.147, train ≥0.300, disjoint by
  construction; CSVs reproduce exactly from seed=11.
- Fixed: dataset table + versioning note in both docs; provenance row in the Phase 3
  note. **Open:** template intro still says "Phase 3 results pending" (now false).

## Methodology chapter swap (evening)

- Adopted the proposed methodology: `template/00Methodology_Vorschlag.tex` content now
  **is** `04Methodology.tex` (old chapter replaced; backup in scratchpad
  `04Methodology_OLD_backup.tex` — template/ isn't git-tracked). Deleted the Vorschlag file.
- 4 ref/label fixes so nothing breaks: added `\label{chap:methodik}` (6 inbound refs),
  `\ref{setup}`→`\ref{chap:setup}` (2×), labelled `sec:meth-xai` + `sec:meth-advtrain`.
- Verified thesis-wide: no dangling refs, no duplicate labels, all 13 citations resolve,
  braces/math/environments balanced. Not compiled (no local LaTeX).
- **Open decision:** chunk-labeling (Min-Overlap) now has a full definition in BOTH
  new `04` (`sec:meth-chunklabeling`) and `05ExperimentalSetup` — user will decide which
  keeps it.

## Notes

- Headline: video-only collapses to ~chance (AUC 0.527) under harsh re-encoding;
  multimodal stays ≥0.741 and wins at all 28 grid cells (+0.195 mean AUC).
  Robustness is audio-anchored (audio AUC 0.921 even at 16 kbps).
- Mechanism: compression biases the video model toward REAL, frame-rate drop
  toward FAKE (opposite directions) — flagged for Phase-3 AttnLRP follow-up.
- Limits: single run/no seeds/no retained per-video scores → no DeLong/p-values;
  CIs are sampling-only.

## Promote Later

- ✅ Drafted the thesis Phase-3 Results section directly in `template/06Results.tex`
  (replaced placeholder; summary table + 3 figures + mechanism + honest-limits prose;
  figures copied to `template/figures/`). Not compiled — no local LaTeX toolchain.
- TODO: mirror the Phase-3 prose into `Writing/results-de.md` if the vault draft
  should stay in sync with the thesis .tex.
- TODO: fill Phase-4 placeholder once adversarial sweeps are analyzed.
- Run `fusion_mode=audio_only` on the same grid to confirm audio-presence (vs
  fusion-design) drives robustness.
