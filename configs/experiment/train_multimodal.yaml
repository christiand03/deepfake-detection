# @package _global_

defaults:
  - override /data: deepfake_multimodal
  - override /model: multimodal
  - override /logger: wandb
  - override /trainer: gpu

ckpt_export_name: multimodal

tags: ["multimodal", "cross-attention", "fusion", "videomae", "wav2vec2"]
